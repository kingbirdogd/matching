###### How to build with CMake

`$ mkdir build && cd build`  <br />
`$ cmake .. && make`

###### Run

`$ cd matching_test/src/` <br />
`$ ./matching_test`

This is a core matching engine Project

1.order input output is same
